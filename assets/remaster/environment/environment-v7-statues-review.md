# Closed static sculpture candidate in q3dm1

Scene `environment-scene-q3dm1-v7-statues.pk3` SHA-256
`6aebc7e04cb521b8c96932aad5bbb285ef8ebcd4458308981dfa40b867f3cb48`.
It extends exterior-correct v6 without changing its models, materials or placements.
Fifteen instances bind42 original surfaces. No BSP/AAS, original textures or
character runtime files are included.

The two statue groups2033–2035 and2036–2043 now use a static sculpture derived
from the character owner's closed master, SHA-256
`6499e2558b63dab69a00dcc604d322673d8e8d0a1e3b1000e62568d91292022c`.
The transferred archive SHA-256 is
`bae69b099cc13d7d243c0bd490d3253b360acc996a4855b38ea010f1f6730050`.
Original paid GLB SHA-256 remains
`da043fbe4bf5136c36db5a58d36aaf03da3eb482d9cac71a3b9772a697cb3c8d`.
No new generation or rigging payment. The old post-decimation open master is
not used. This reuses approved Sarge identity, not an exact Visor/Major statue
replica or character runtime acceptance.

Blender evaluates walk frame2, re-grounds the posed body, assigns original matte
ivory/graphite stone, clears obsolete posed custom normals, and preserves source
loop UVs. Shared CW exporter emits3000 triangles across4 MD3 surfaces. Explicit
quantized-degenerate cleaning removes **zero** triangles. Independent exact
1/64-grid edge incidence after export: boundary0, nonmanifold0, with UV seam
coordinates identified only for measurement, never welded in the runtime file.
The packager refuses open output; tests distinguish closed split-UV tetrahedron,
a missing face and a duplicated face.

Static local front is+X. East yaw180/west yaw0 face the hall. Uniform fitting
retains each original group union envelope, but Z is bottom-aligned to original
feet, not centered inside unequal-height bounds. Placement JSON has no new
renderer fields; grounding is a producer-side operation. Source collision/PVS
and original pedestal stay unchanged.

Actual WASM `review_lanterns.mjs CDP OUT statues`:8 PASS, four front/oblique
cameras × HDR1/HDR0,42 binding logs per load, actual300ms lateral key input and
>10-unit observed movement, no context loss. Inspected west LDR front and east
HDR oblique: supported feet, contiguous silhouette, no duplicate original
statue or detached fragments. Paths `.amp/in/artifacts/environment-v7-statues/`.
Thirteen environment tests pass. These noclip views do not verify physical
collision/navigation, all PVS boundaries or a complete Bot match.

Art remains unfinished: pale body loses contrast against sky, head/hand details
are soft and some limb transitions angular. Both poses share one generated
identity; this is not the full original statue identity/pose coverage. Bright
room lighting still needs a measured refinement, not arbitrary model gain.
q3dm1 has2055 original surface geometries remaining and full-map gates false.

E5 pilaster task `task_6ZmyFVL4BZwnzRXkkcKcdo87NLGdQkup` completed once;
billing matched its exact request ID at$0.50. Painter cost remains unknown.
The first export is too deep (~34 vs8 allowed units) and dark on all inspected
faces. It is retained for provenance but **not included** in v7; Blender shape
and material refinement is required before measured architectural placement.
